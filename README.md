# ReadStack 📚

ReadStack is a full-stack web application designed for avid readers to track their reading journey. It allows users to create a personal library of books, complete with cover art and AI-generated summaries, ensuring you never forget the key details of what you've read.

-----

## \#\# About The Project

Forgetting the plot twists, main characters, or the core message of a book you read months ago is a common problem for readers. ReadStack solves this by providing a simple and elegant platform to log your books. By just entering a book's title and author, the application generates a beautiful card containing the book's cover and a unique summary crafted by Google's Gemini AI. This allows you to quickly recall the essence of any book in your collection.

-----

## \#\# Key Features

  * **👤 User Authentication:** Secure user registration and login with password hashing using **bcrypt**.
  * **📚 Personalized Bookshelves:** Add books to your personal profile to track what you've read.
  * **🤖 AI-Powered Summaries:** Get unique, concise summaries for your books generated by the **Google Gemini API**.
  * **🖼️ Automatic Cover Art:** Book covers are automatically fetched from the **Open Library API**.
  * **⚙️ Full CRUD Functionality:** Easily **C**reate, **R**ead, **U**pdate, and **D**elete book entries on your profile.
  * **🔐 Session Management:** Persistent user sessions using `express-session` keep you logged in across browser visits.

-----

## \#\# Tech Stack

This project is built with a modern web development stack:

  * **Frontend:** EJS (Embedded JavaScript templates), CSS
  * **Backend:** Node.js, Express.js
  * **Database:** PostgreSQL
  * **APIs:** Google Gemini API, Open Library API
  * **Authentication:** bcrypt, express-session

-----

## \#\# Getting Started

To get a local copy up and running, follow these simple steps.

### \#\#\# Prerequisites

Make sure you have the following installed on your machine:

  * Node.js
  * npm (Node Package Manager)
  * PostgreSQL

### \#\#\# Installation

1.  **Clone the repository:**
    ```sh
    git clone https://github.com/ARCoder181105/ReadStack
    ```
2.  **Navigate to the project directory:**
    ```sh
    cd ReadStack
    ```
3.  **Install NPM packages:**
    ```sh
    npm install
    ```
4.  **Set up environment variables:**
    Create a `.env` file in the root of your project and add the following variables. This file should not be committed to Git.
    ```env
    # PostgreSQL Database Connection
    DB_USER=your_postgres_username
    DB_HOST=localhost
    DB_DATABASE=your_database_name
    DB_PASSWORD=your_postgres_password
    DB_PORT=5432

    # Express Session
    SESSION_SECRET=a_very_strong_and_long_secret_key

    # Google Gemini API
    GEMINI_API_KEY=your_gemini_api_key
    ```
5.  **Run the application:**
    ```sh
    npm start
    ```
    Now, open your browser and navigate to `http://localhost:3000`.

-----

## \#\# ⚠️ Important Note on Database Hosting

This project is currently deployed using a free-tier PostgreSQL instance which has a limited trial period (approximately 3 months).

If the live site is inaccessible, it is likely because the database service has expired. I will manually migrate the data and redeploy the application to a new instance as soon as I am aware of the issue. **All user data will be preserved** during this process. Your patience is appreciated\!